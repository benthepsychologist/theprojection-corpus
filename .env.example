# Copy to .env and fill in. .env is gitignored — never commit real values.
# Read by publish/adapter.py — the site checkout path and (optional)
# Cloudflare Workers Builds deploy hook this adapter fires on `--push`.
THEPROJECTION_DEPLOY_HOOK=
THEPROJECTION_SITE_DIR=
